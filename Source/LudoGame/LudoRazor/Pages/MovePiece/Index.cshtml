@page
@model LudoRazor.Pages.MovePiece.IndexModel

@{
    ViewData["Title"] = "Index";
}

<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayName("Id")
        </th>
        <th>
            @Html.DisplayName("Color")
        </th>
        <th>
            @Html.DisplayName("Steps")
        </th>
        <th>
            @Html.DisplayName("Position")
        </th>
        <th></th>
    </tr>
    </thead>
    
    <tbody>
    @foreach (var piece in Model.Pieces)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => piece.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => piece.Color)
            </td>
            <td>
                @Html.DisplayFor(modelItem => piece.Steps)
            </td>
            <td>
                @Html.DisplayFor(modelItem => piece.Position)
            </td>

            @*<td><button id="move-@piece.Id" class="btn btn-primary" onclick="movePiece(@piece.Id)">Choose Piece</button></td>*@
            @*<td>
                <a asp-page="/Movepiece/index" asp-route-id="@Model.GameBoard.Id" asp-route-pieceId="@piece.Id" asp-route-dieValue="@Model.Die" class="btn btn-primary">Move piece</a>

            </td>*@
            

            @*<script>
            function movePiece(input) {


                    const data = {​​ username: 'example' }​​;fetch('http://localhost:5000', {
                    method: 'POST', // or 'PUT'
                    headers: {​​'Content-Type': 'application/json',}​​,
                    body: JSON.stringify(data),}​​).then(response => response.json()).then(data => {
                    console.log('Success:', data);}​​).catch((error) => {
                    console.error('Error:', error);}​​);





            }


        </script>*@
        </tr>
    }
    </tbody>
    
    
</table>


<a asp-page="/NextPlayer/Index" asp-route-gameId="@Model.GameBoard.Id" class="btn btn-primary">Next player</a>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
